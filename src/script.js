// Exemple de code JavaScript basique

// Sélectionnez un élément HTML (par exemple, une balise <div> avec l'ID "app")
const appElement = document.getElementById('app')

// Créez une fonction qui met à jour le contenu de l'élément sélectionné
function updateContent () {
  appElement.textContent = 'Bonjour, Monde !'
}

// Appelez la fonction pour mettre à jour le contenu de l'élément
updateContent()
